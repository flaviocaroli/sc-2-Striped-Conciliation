run_name: "census_autoencoder_first_train"
seed: 42
device: "auto"

data:
  n_genes: 2048
  log1p_input: true
  batch_size: 64
  num_workers: 0

model:
  hidden_dims: [512, 128]
  dropout: 0.1

train:
  epochs: 8
  lr: 0.001
  weight_decay: 0.0001
  grad_clip_norm: 1.0